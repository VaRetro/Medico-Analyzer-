# Example environment variables (DO NOT store secrets here in production)
# Client-side Vite variables must be prefixed with VITE_
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=public-anon-key

# Optional server-side keys (leave blank here - set in your hosting provider or GitHub Actions secrets)
LOVABLE_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# NOTE: Never commit real secret keys to the repository. Use provider/environment secrets for CI and hosting.
